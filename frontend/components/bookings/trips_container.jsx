import { connect } from 'react-redux'; 
import Trips from './trips';

const msp = (state) => {
    return({

    })
}; 


const mdp = (dispatch) => {
    return({

    })
}

export default connect(msp, mdp)(Trips);